
export async function parsePdf(file: File): Promise<string> {
  // Demo fallback: in real usage, use pdf.js or similar; here, just return placeholder
  return "Fallback parsed text from resume PDF.";
}
